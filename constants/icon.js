import Feather from 'react-native-vector-icons/Feather';

export const icons = {
  Home: (props) => <Feather name="home" {...props} />,
  Appointments: (props) => <Feather name="calendar" {...props} />,
  Profile: (props) => <Feather name="user" {...props} />,
  
};
